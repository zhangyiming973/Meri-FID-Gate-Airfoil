# Meri-FID-Gate 项目全面总结与实验报告

> 日期：2026-06-15  
> 环境：Ubuntu Linux, Python 3.10+, PyTorch (CUDA)  
> 分支：`airfoil-conditional-diffusion`  
> 编译环境：`/home/vipuser/conda_envs/xfoil-build` (gfortran + cmake + X11)

---

## 目录

1. [项目群概述](#1-项目群概述)
2. [项目一：Meri-FID-Gate-Airfoil — 核心项目](#2-项目一meri-fid-gate-airfoil--核心项目)
3. [项目二：sdfdata — NX PDF 数据管线](#3-项目二sdfdata--nx-pdf-数据管线)
4. [项目三：NOTtest — 神经网络算子](#4-项目三nottest--神经网络算子)
5. [项目四：XFOIL 气动校核环境](#5-项目四xfoil-气动校核环境)
6. [UIUC 翼型条件扩散完整实验链](#6-uiuc-翼型条件扩散完整实验链)
7. [实验指标汇总](#7-实验指标汇总)
8. [产物与文件清单](#8-产物与文件清单)
9. [环境与依赖](#9-环境与依赖)
10. [已知问题与改进方向](#10-已知问题与改进方向)

---

## 1. 项目群概述

本工作台（`/home/vipuser`）包含四个关联的项目仓库，共同构成一条从 **NX CAD 参数化建模 → PDF 几何提取 → SDF 数据管线 → 条件扩散生成 → XFOIL 气动校核** 的完整链路。

| 项目 | 路径 | 角色 |
|------|------|------|
| **Meri-FID-Gate-Airfoil** | `~/Meri-FID-Gate-Airfoil` | 核心：条件扩散生成 + 翼型实验 |
| **sdfdata** | `~/sdfdata` | 上游：NX PDF → SDF 数据管线 |
| **NOTtest** | `~/NOTtest` | 下游：神经网络算子 FEM 代理 |
| **XFOIL** | `~/tools/xfoil` | 工具：翼型气动校核求解器 |

关系图：

```
sdfdata                      Meri-FID-Gate-Airfoil           NOTtest
NX PDF → SDF/NPZ ──────────→ 条件扩散生成翼型 SDF ──────────→ (可接入)
                数据输入         │                                  │
                           XFOIL 气动校核                    FEM 代理模型
                           tools/xfoil
```

---

## 2. 项目一：Meri-FID-Gate-Airfoil — 核心项目

### 2.1 项目定位

基于**条件扩散模型**的几何生成系统。原始任务为子午面 SDF 尺寸引导生成（5 维设计参数 → 256×256 SDF），后续扩展为 **UIUC 翼型条件扩散生成**（5 维几何条件 → 翼型 SDF → 翼型坐标）。

### 2.2 代码结构

```
Meri-FID-Gate-Airfoil/
├── run.py                          # 统一入口：prepare-splits / train / test / collect-timing
├── scripts/
│   ├── airfoil_geometry.py         # 翼型几何核心（解析 / 归一化 / 拆分 / 条件计算）
│   ├── airfoil_sdf.py              # 翼型 SDF 栅格化（多边形 → 符号距离场）
│   ├── prepare_airfoil_uiuc.py     # UIUC 预处理流水线（批量 .dat → .npz）
│   ├── split_utils.py              # train/test 划分工具
│   ├── timing_utils.py             # 训练/测试计时
│   ├── prepare_splits.py           # 固定划分生成
│   ├── airfoil/
│   │   ├── run_xfoil_batch.py      # XFOIL 批量气动校核（并行多进程）
│   │   ├── evaluate_generated_airfoils.py  # 几何校核与重排
│   │   └── plot_xfoil_results.py   # XFOIL 结果可视化
├── data/
│   ├── single/                     # 单样本子午面数据集（五维条件方差充足）
│   │   ├── dataset.json
│   │   └── processed/
│   ├── F404/                       # F404 子午面数据集（部分条件列为常量）
│   │   ├── dataset.json
│   │   └── processed/
│   ├── airfoil_uiuc_sdf/           # 翼型 SDF 数据集
│   │   ├── dataset.json
│   │   └── processed/
│   │       ├── airfoil_index.csv   # 全量索引（1646 条）
│   │       ├── train_split.csv     # 训练集 1399 条
│   │       ├── test_split.csv      # 测试集 247 条
│   │       ├── condition_stats.json
│   │       ├── filter_report.json
│   │       └── airfoil_samples/    # 1646 个 .npz
│   └── airfoil/raw/uiuc/          # UIUC 原始 .dat 翼型库（~1665 个）
├── schemes/                        # 6 套独立扩散方案
│   ├── mlp/                        # 基线：PCA(128) + MLP 去噪器
│   ├── pca_unet/                   # PCA(128) → 网格 + UNet 去噪器
│   ├── dim_guided/                 # 尺寸引导 MLP 扩散（ConditionVector 校验）
│   ├── dim_unet/                   # 尺寸引导 UNet 扩散（PCA 192 + 条件 UNet）
│   ├── edm_guided/                 # EDM 预条件化（Karras）+ Heun 采样
│   └── pidm_guided/               # PIDM 物理信息扩散（几何残差虚拟似然）
├── outputs/                        # 所有训练/测试输出
│   ├── mlp/{single,F404}/
│   ├── pca_unet/{F404,airfoil_uiuc_sdf}/
│   ├── dim_guided/{single,F404}/
│   ├── dim_unet/{single,F404}/
│   ├── edm_guided/{single,F404}/
│   └── pidm_guided/{single,F404}/
├── tests/                          # 19 个 airfoil 相关测试 + 子午面测试
├── docs/                           # 项目文档
│   ├── airfoil_experiment_steps.md
│   ├── airfoil_xfoil_experiment_record.md
│   └── airfoil_gpu_runbook.md
├── wenxian/                        # 参考文献（EDM / PIDM 论文）
├── 实验方案.md                       # UIUC 翼型实验总体设计
├── 编码计划.md                       # 五阶段编码实施计划
└── requirements.txt
```

### 2.3 六套扩散方案对比

| 方案 | `--scheme` | 去噪器 | PCA 维度 | 扩散框架 | 采样 | 条件校验 | 物理引导 |
|------|-----------|--------|---------|----------|------|----------|----------|
| MLP 基线 | `mlp` | MLP (512×4层) | 128 | DDPM | DDIM 50步 | 无 | 可选 |
| PCA-UNet | `pca_unet` | 条件 UNet (网格) | 128/256 | DDPM | DDIM 50步 | 无 | 无 |
| 尺寸引导 | `dim_guided` | MLP | 128 | DDPM | DDIM 50步 | 有 | 可选 |
| 尺寸UNet | `dim_unet` | 条件 UNet (网格) | 192 | DDPM | DDIM 80步 | 有 | 可选 |
| EDM引导 | `edm_guided` | EDM预条件化MLP | 128 | EDM | Heun 35步 | 有 | 可选 |
| PIDM物理 | `pidm_guided` | MLP | 128 | DDPM | DDIM 50步 | 有 | PIDM虚拟似然 |

### 2.4 核心技术架构

**三阶段管线**：

```
阶段 1：自编码器 (AE)
  SDF + Semantic Mask → Encoder(4层Conv+MaxPool) → z_m(64×16×16) → Decoder → SDF_recon

阶段 2：质量门控 (Gate)
  校验重建 L1 ≤ 0.08、潜空间 std ≥ 0.05、通过率 ≥ 85%

阶段 3：条件扩散 (Diffusion)
  z_m → PCA(128~256维) → 条件去噪网络 → DDIM/Heun采样 → PCA⁻¹ → AE Decoder → SDF_gen
```

**扩散训练核心机制**：

- **Classifier-Free Guidance (CFG)**：训练时 20% 概率丢弃条件，推理时加权混合
- **DDIM 采样**：确定性跳步加速，200 → 50/80 步
- **EDM 预条件化**：c_in/c_out/c_skip/c_noise 保持训练目标尺度恒定
- **PIDM 虚拟似然**：几何残差（壁厚+Eikonal）独立约束，目标 r→0

**五维条件向量**：

| 字段 | 含义 | 单位 |
|------|------|------|
| `t_max` | 最大厚度 | 弦长比 |
| `x_tmax` | 最大厚度位置 | 弦长比 |
| `camber_max` | 最大弯度 | 弦长比 |
| `x_camber_max` | 最大弯度位置 | 弦长比 |
| `te_gap` | 尾缘厚度 | 弦长比 |

### 2.5 子午面实验指标（single / F404 数据集）

| 方案 | single (fast) mean_gen_l1 | F404 (full) mean_gen_l1 | 说明 |
|------|--------------------------|------------------------|------|
| MLP | ~0.036 | ~0.037 | 基线 |
| PCA-UNet | ~0.014 | ~0.030 | UNet 精度最高 |
| dim_guided | ~0.038 | ~0.043 | 含条件校验 |
| dim_unet | — | ~0.025 | UNet+192维+F404 |
| edm_guided | ~0.030 | ~0.033 | Heun 采样 |
| pidm_guided | ~0.033 | ~0.039 | 物理残差 ~0.049 |

---

## 3. 项目二：sdfdata — NX PDF 数据管线

### 3.1 项目定位

从 NX 原生矢量 PDF 提取子午面轮廓，生成 SDF + 语义 mask + 工程曲线 + 物理代理标签的完整数据管线。

### 3.2 关键能力

- **矢量 PDF 直接解析**：不依赖 PyMuPDF/pypdf/shapely，用 `zlib` 解压 `/FlateDecode` 流，解析 NX 发出的 m/l/c/S/f* 等基本绘图操作
- **Euler 遍历闭合轮廓重建**：处理 NX 局部重叠/退化线段
- **自动坐标系还原**：PDF 坐标 → 真实 r-z 物理坐标
- **完整的质量检查链**：40+ 种 reason code 覆盖 PDF/几何/SDF/语义各环节

### 3.3 主要脚本

| 脚本 | 功能 |
|------|------|
| `pipeline_common.py` | 核心实现：PDF 解析 / 轮廓提取 / SDF 生成 / 工程曲线 / 预览图 |
| `build_condition_specs.py` | 从 design_150.csv + paramentset.xlsx 生成条件规格 |
| `build_sdf_samples.py` | 主处理入口：PDF → contour → SDF → NPZ |
| `run_quality_report.py` | 生成 data_quality_report.md |
| `jaxfem_axisym_thermo_mech_oneway.py` | JAX-FEM 轴对称热-力单向耦合求解 |
| `jaxfem_axisym_spin_minimal.py` | 最简轴对称旋转 FEM |
| `ge90_axisym_pdf_workflow.py` | GE90 轴对称 PDF → FEM 完整工作流 |
| `run_ge90_pdf_batch.py` | GE90 批处理驱动 |

### 3.4 数据集规格

- **设计点**：150 个拉丁超立方抽样（5 参数变化：`angle_web_deg` / `r_bore2_mm` / `r_outer2_mm` / `r_trans_bore_web_mm` / `z_min`）
- **数据量**：150/150 全部通过质量检查，0 失败
- **SDF 规格**：256×256，clip ±12mm，内部为负
- **语义分区**：hub / web / rim 三区域

---

## 4. 项目三：NOTtest — 神经网络算子

### 4.1 项目定位

基于 Neural Operator Transformer 的轴对称盘类零件 FEM 代理模型。支持标量输出（质量、最大应力、位移、安全裕度）和场输出（位移场、应力场）。

### 4.2 代码结构

```
NOTtest/
├── src/disk_not/                    # Python 包
│   ├── data/                        # 数据集（field/scalar/conditions）
│   ├── models/                      # 模型（FNOGNO / ScalarCNN / NOT）
│   ├── losses/                      # 损失（scalar/field）
│   ├── eval/                        # 评估指标
│   ├── train/                       # 训练循环
│   └── api/                         # 推理接口（ScalarNotPredictor / FieldNotPredictor）
├── scripts/                         # CLI 入口
│   ├── train_not_scalar_smoke.py
│   ├── train_fnogno_field_smoke.py
│   ├── eval_not_scalar.py
│   ├── eval_fnogno_field.py
│   └── prepare_scalar_labels_from_jaxfem_summary.py
├── GE90HPTSTG1_1000/                # GE90 1000 样本数据管线
│   ├── generate_ge90_1000_design.py
│   ├── build_sdf_samples.py
│   ├── build_condition_specs.py
│   ├── design_1000.csv
│   └── scripts/                     # 同 sdfdata 镜像的 PDF → SDF 管线
├── runs/                            # 训练产物
│   ├── fnogno_ablation_summary.csv
│   └── fnogno_t1_8ep_sigma3_bs1_nq4096_rerun_newgpu/
│       ├── best_model.pt
│       ├── config.json
│       └── train.log
└── docs/                            # 交接文档
```

### 4.3 关键指标

- **FEM 样本**：990 个标量标签（`status == ok`），0 失败
- **场数据**：990 个位移/应力场 NPZ
- **条件向量**：rpm, E, nu, rho, sigma_allow, u_allow
- **FEM 输出**：~12 GB 原始结果，~7.3 GB 处理后 NOT 数据集

---

## 5. 项目四：XFOIL 气动校核环境

### 5.1 构建信息

| 项目 | 值 |
|------|-----|
| 源码 | `https://github.com/RobotLocomotion/xfoil` (commit `d11a1544`) |
| 可执行文件 | `/home/vipuser/tools/xfoil/build/src/xfoil-6.97` |
| 编译器 | gfortran (via conda-forge `x86_64-conda-linux-gnu-gfortran`) |
| 构建系统 | CMake |
| Conda 环境 | `/home/vipuser/conda_envs/xfoil-build` |
| 环境包 | gfortran_linux-64, cmake, make, xorg-libx11, xorg-xorgproto |

### 5.2 批量校核脚本能力

`scripts/airfoil/run_xfoil_batch.py` 支持：

- 自动配对生成翼型与 UIUC 原始同名翼型
- CPU 多进程并行 XFOIL 求解
- 自动关闭 X11 图形（PLOP/G 命令）
- 几何清洗：cosine 重采样、camber/thickness 平滑、尾缘渐变、最小厚度夹紧
- 输出：polar 文件、汇总 CSV、配对对比 CSV、JSON/HTML 报告

---

## 6. UIUC 翼型条件扩散完整实验链

### 6.1 实施里程碑回顾

本实验按编码计划的五阶段执行：

#### M1：数据预处理 ✅

```bash
python run.py prepare-airfoil-uiuc --force
python run.py prepare-splits --dataset airfoil_uiuc_sdf --force
```

| 指标 | 值 |
|------|-----|
| 原始 UIUC 文件 | ~1665 个 .dat |
| 通过过滤 | 1646 |
| 失败/跳过 | 19（异常坐标/过薄/过厚/插值失败等） |
| 训练集 | 1399 |
| 测试集 | 247 (15%) |
| 条件维度 | t_max, x_tmax, camber_max, x_camber_max, te_gap |
| SDF 分辨率 | 128×256 |

#### M2：AE 训练 ✅

```bash
python run.py train --scheme pca_unet --dataset airfoil_uiuc_sdf --stage ae
```

| 指标 | 值 |
|------|-----|
| AE 架构 | 4 层 Conv + MaxPool encoder, 镜像 decoder |
| 输入尺寸 | [128, 256] |
| 潜空间 | [64, 8, 16] |
| Epochs | 80 |
| 最终 val L1 | **0.00354** |
| 几何 pass ratio | **100%** |
| 输出目录 | `outputs/pca_unet/airfoil_uiuc_sdf/20260615_110639/autoencoder/` |

#### M3：条件扩散训练 ✅

```bash
python run.py train --scheme pca_unet --dataset airfoil_uiuc_sdf --stage unet \
  --ae-run-dir outputs/pca_unet/airfoil_uiuc_sdf/20260615_110639/autoencoder
```

| 指标 | 值 |
|------|-----|
| 去噪器 | 条件 UNet（PCA 256维 → 1×16×16 网格） |
| PCA 维度 | 256 |
| Epochs | 200 |
| Batch size | 16 |
| CFG 配置 | dropout=0.2, scale=1.5 |
| 采样 | DDIM 50 步 |
| 训练耗时 | **28分54秒** |
| 测试集生成 L1 | **0.0374** |
| 输出目录 | `outputs/pca_unet/airfoil_uiuc_sdf/20260615_114115/` |

#### M4：几何校核与重排 ✅

```bash
python scripts/airfoil/evaluate_generated_airfoils.py \
  --run-dir outputs/pca_unet/airfoil_uiuc_sdf/20260615_114115 \
  --num-per-condition 32
```

- 每个测试条件采样 32 个候选翼型
- 从 SDF 提取零水平集 → 轮廓清理 → 上下表面拆分 → 条件计算
- 按归一化条件误差排序，选出最优候选

| 重排方案 | 每条件候选数 | 产物 |
|----------|------------|------|
| `geometry_eval_rerank_c1` | 1 | 基础生成 |
| `geometry_eval_rerank_c32` | 32 | 条件误差最小化重排 |

**Top 5 几何最优**（c32 重排）：

| rank | sample_id | 条件误差 score | abs t_max err | abs camber err | abs te_gap err |
|---:|---:|---:|---:|---:|---:|
| 1 | dga1138 | 0.702 | 0.0003 | 0.0030 | 0.0059 |
| 2 | goe188 | 0.851 | 0.0006 | 0.0047 | 0.0045 |
| 3 | fg2 | 0.936 | 0.0038 | 0.0029 | 0.0121 |
| 4 | m14 | 1.032 | 0.0001 | 0.0051 | 0.0090 |
| 5 | n6h10 | 1.206 | 0.0002 | 0.0047 | 0.0088 |

#### M5：XFOIL 气动校核 ✅

```bash
/home/vipuser/miniconda3/bin/python scripts/airfoil/run_xfoil_batch.py \
  --xfoil-bin /home/vipuser/tools/xfoil/build/src/xfoil-6.97 \
  --generated-dir .../geometry_eval_rerank_c32/top_dat \
  --baseline-raw-dir data/airfoil/raw/uiuc \
  --ranked-csv .../rerank_summary.csv \
  --out-dir .../xfoil_eval_top50_re1e6_m010_a-2_6 \
  --limit 50 --workers 16 --alpha-start -2 --alpha-end 6 --alpha-step 1 \
  --smooth-passes 8 --te-gap 0.001 --te-blend-start 0.90 --timeout 35 --force
```

**工况**：Re=1e6, Mach=0.10, Ncrit=9, α=-2°~6° (步长 1°)

### 6.2 XFOIL 校核结果

| 指标 | 生成翼型 | UIUC 原始同名翼型 |
|------|---------:|-----------------:|
| 有效 polar | 40/50 | 41/50 |
| 完整 alpha sweep (9 点) | 9/50 | 23/50 |
| 平均 best L/D | 30.89 | **102.71** |
| 中位 best L/D | 23.54 | 96.34 |
| 平均 max CL | 0.553 | 0.921 |

**有效配对** (32 对)：平均 Δ best L/D = -73.42，生成优于原始的仅 1/32。

### 6.3 Top 生成翼型（按 XFOIL best L/D）

| rank | sample_id | best L/D | alpha | max CL | 收敛点数 |
|---:|---|---:|---:|---:|---:|
| 1 | **goe188** | 103.41 | 5.0° | 0.930 | 9/9 |
| 2 | **m14** | 94.14 | 6.0° | 0.915 | 9/9 |
| 3 | **fx74cl6140** | 88.47 | 6.0° | 1.082 | 9/9 |
| 4 | goe346 | 76.16 | 1.0° | 0.224 | 4/9 |
| 5 | goe322 | 74.69 | 5.0° | 1.065 | 8/9 |
| 6 | ua2-180 | 57.16 | 3.0° | 0.879 | 9/9 |
| 7 | pmc19sm | 55.77 | 6.0° | 0.863 | 8/9 |
| 8 | rc08n1 | 49.66 | 6.0° | 0.781 | 9/9 |
| 9 | fx66h60 | 48.82 | 6.0° | 0.707 | 8/9 |
| 10 | isa960 | 47.86 | 2.0° | 0.613 | 6/9 |

### 6.4 XFOIL 实验工程问题与修正

在实验过程中解决了以下关键问题：

1. **XFOIL 长路径读取失败**：每个 case 使用独立工作目录 + 短文件名 `airfoil.dat`
2. **无 DISPLAY 环境 X11 崩溃**：批处理输入开头插入 `PLOP\nG\n\n` 关闭图形
3. **生成翼型点数超 XFOIL 限制**：对送入 XFOIL 的几何做 cosine 重采样至 161 点/面（共 321 点）
4. **Viscous BL 不收敛**：加入 camber/thickness 平滑、最小厚度夹紧、尾缘从 x/c=0.90 平滑过渡到 0.001c

---

## 7. 实验指标汇总

### 7.1 数据阶段

| 指标 | 值 |
|------|-----|
| UIUC 有效翼型 | **1646 / 1665** (98.9%) |
| 训练/测试划分 | 1399 / 247 (85/15) |
| SDF 分辨率 | 128 × 256 |
| 条件维度 | 5 (t_max, x_tmax, camber_max, x_camber_max, te_gap) |

### 7.2 训练阶段

| 阶段 | 指标 | 值 |
|------|------|-----|
| AE | val L1 | **0.00354** |
| AE | 几何 pass ratio | **100%** |
| 扩散 | 训练时间 | 28分54秒 |
| 扩散 | PCA 维度 | 256 |
| 扩散 | 测试集 gen L1 | **0.0374** |

### 7.3 评估阶段

| 评估维度 | 值 |
|----------|-----|
| 生成翼型 XFOIL 收敛率 | 80% (40/50) |
| 生成平均 best L/D | 30.89 |
| 原始平均 best L/D | 102.71 |
| 生成平均 max CL | 0.553 |
| 原始平均 max CL | 0.921 |
| 最优生成 L/D | **103.41** (goe188) |
| 最优生成 max CL | **1.082** (fx74cl6140) |

---

## 8. 产物与文件清单

### 8.1 核心实验产出

```
outputs/pca_unet/airfoil_uiuc_sdf/20260615_114115/
├── diffusion/
│   ├── best_unet_diffusion.pt            # 扩散模型权重
│   ├── latent_pca.json                   # PCA 参数
│   ├── generation_metrics.json           # 逐样本生成指标
│   ├── summary.json                      # 汇总（mean_gen_l1=0.0374）
│   ├── training_history.json             # 逐 epoch 损失
│   ├── training_dashboard.png            # 训练面板
│   ├── training_curves.png              # 训练曲线
│   ├── grid_test.png                     # 测试网格对比
│   ├── metrics_test.png                  # 逐样本指标图
│   └── generations/                      # 247 个样本生成 PNG
├── geometry_eval_rerank_c32/
│   ├── rerank_summary.csv                # 重排汇总
│   ├── per_sample_geometry.csv           # 逐样本几何
│   ├── geometry_eval_report.json         # 评估报告
│   ├── best_airfoils_for_aero.md         # 气动校核推荐列表
│   ├── candidate_dat/                    # 全部候选 .dat
│   └── top_dat/                          # Top 翼型 .dat（用于 XFOIL）
├── xfoil_eval_top50_re1e6_m010_a-2_6/
│   ├── xfoil_report.md                   # 详细报告
│   ├── xfoil_report.json                 # JSON 报告
│   ├── xfoil_conclusion_cn.md            # 中文结论
│   ├── xfoil_summary.csv                 # 全部 case 汇总
│   ├── xfoil_paired_comparison.csv       # 配对对比
│   ├── visualizations/
│   │   ├── visualization_index.md
│   │   ├── xfoil_convergence_summary.png
│   │   ├── xfoil_best_ld_distribution.png
│   │   ├── xfoil_paired_metric_scatter.png
│   │   ├── xfoil_top_generated_best_ld.png
│   │   ├── xfoil_top_generated_polar_curves.png
│   │   └── xfoil_top_generated_airfoil_shapes.png
│   └── cases/                            # 100 个 case 目录
├── visualizations/
│   ├── summary_report.json
│   ├── timing.json
│   ├── test_generation_l1.png
│   ├── test_ae_recon_l1.png
│   ├── test_gen_vs_ae_recon.png
│   ├── diff_training_dashboard.png
│   ├── diff_training_curves.png
│   ├── diff_grid_test.png
│   └── generations/                      # 247 个三联对比图
├── logs/train.log
└── timing.json
```

### 8.2 关键代码（本次实验新增）

| 文件 | 说明 | 提交 |
|------|------|------|
| `scripts/airfoil_geometry.py` | 翼型解析/归一化/条件计算 | `8c57981` |
| `scripts/airfoil_sdf.py` | SDF 栅格化 | `981eb13` |
| `scripts/prepare_airfoil_uiuc.py` | UIUC 预处理入口 | `a2f530a` |
| `data/airfoil_uiuc_sdf/dataset.json` | 数据集配置 | `a2f530a` |
| `schemes/pca_unet/config/airfoil_uiuc_sdf.json` | 翼型训练配置 | `a2f530a` |
| `scripts/airfoil/run_xfoil_batch.py` | XFOIL 批量校核 | `9a6f5f8` |
| `scripts/airfoil/evaluate_generated_airfoils.py` | 几何校核重排 | `94f379b` |
| `scripts/airfoil/plot_xfoil_results.py` | XFOIL 可视化 | `01121ba` |
| `docs/airfoil_xfoil_experiment_record.md` | 实验记录 | `4155620` |

### 8.3 Git 提交历史 (airfoil-conditional-diffusion 分支)

```
01121ba Add XFOIL result visualizations
4155620 Document XFOIL airfoil evaluation
9a6f5f8 Add batch XFOIL airfoil evaluation
94f379b Add airfoil geometry rerank export
bb805f9 Prepare pca_unet airfoil diffusion stage
262676f Add pca_unet airfoil dataset smoke tests
a2f530a Add UIUC airfoil preprocessing pipeline
981eb13 Add airfoil SDF rasterization
8c57981 Add airfoil geometry primitives
103fc54 Fix pca_unet airfoil dataset assumptions
```

---

## 9. 环境与依赖

### 9.1 XFOIL 构建环境

```bash
# 激活环境
source /home/vipuser/miniconda3/bin/activate /home/vipuser/conda_envs/xfoil-build

# 环境包
gfortran_linux-64 (conda-forge)
cmake
make
xorg-libx11
xorg-xorgproto

# 可执行文件
/home/vipuser/tools/xfoil/build/src/xfoil-6.97
```

### 9.2 Python 环境

```bash
# Python 路径
/home/vipuser/miniconda3/bin/python

# 核心依赖
torch>=2.0
numpy
pandas
tqdm
matplotlib
scikit-learn
openpyxl
```

### 9.3 Conda 环境列表

| 环境 | 用途 |
|------|------|
| `base` (`/home/vipuser/miniconda3`) | 主 Python 环境 |
| `jaxfem` (`/home/vipuser/miniconda3/envs/jaxfem`) | JAX-FEM 求解 |
| `xfoil-build` (`/home/vipuser/conda_envs/xfoil-build`) | XFOIL 编译工具链 |

---

## 10. 已知问题与改进方向

### 10.1 当前主要发现

1. **SDF → 翼型气动可用性存在差距**：几何合法（平均条件误差 < 0.01 弦长比）不等于气动可用。生成翼型的局部曲率、尾缘区域、厚度/弯度分布显著影响 XFOIL 粘性求解。

2. **XFOIL 收敛对几何质量敏感**：即使原始 UIUC 同名翼型也只有 41/50 XFOIL 收敛，说明 XFOOL 本身对点列密度、光滑度和尾缘处理有较高要求。

3. **条件扩散能有效捕捉厚度/弯度差异**：不同条件下生成形状有可见响应，mean_gen_l1 0.037 水平合理。

### 10.2 后续最高收益方向（按优先级）

1. **XFOIL/曲率友好的几何后处理加入训练闭环**
   - 表面曲率平滑损失
   - 尾缘厚度约束和楔角约束
   - 上下表面点列坐标损失（替代纯 SDF 像素损失）
   - thickness/camber 分布损失

2. **采样后气动 rerank/rejection**
   - 几何过滤 → XFOIL 快速筛选 → CFD 精选
   - 以 XFOIL 成功率、best L/D、条件误差综合排序

3. **对 top 样本做更高保真 CFD 验证**
   - goe188 (L/D=103), m14 (L/D=94), fx74cl6140 (L/D=88), goe322 (L/D=75)

4. **训练数据扩展**
   - 加入 G2Aero curated airfoils 增加训练集规模
   - 数据增强：厚度/弯度缩放

### 10.3 当前机器限制

- 当前 Ubuntu 机器无 GPU/CUDA
- 无 `sudo` 权限（conda 环境解决工具链依赖）
- 无 DISPLAY 环境（XFOIL 通过 PLOP/G 关闭图形解决）

---

## 附录：完整文件打包清单

已打包为 `~/airfoil_experiment_results_20260615.tar.gz`，包含以下目录：

| 打包内容 | 路径 |
|----------|------|
| 核心实验输出 | `Meri-FID-Gate-Airfoil/outputs/pca_unet/airfoil_uiuc_sdf/20260615_114115/` |
| XFOIL 全部评估结果 | 上述路径中的 `xfoil_eval_top50_re1e6_m010_a-2_6/` |
| AE 训练输出 | `Meri-FID-Gate-Airfoil/outputs/pca_unet/airfoil_uiuc_sdf/20260615_110639/` |
| 关键代码脚本 | `Meri-FID-Gate-Airfoil/scripts/airfoil/` |
| 所有项目文档 | `Meri-FID-Gate-Airfoil/docs/`, `实验方案.md`, `编码计划.md`, `README.md` |
| 技术文档 | `MERI_FID_GATE_TECHNICAL_DOC.md` |
| 数据集元信息 | `Meri-FID-Gate-Airfoil/data/airfoil_uiuc_sdf/processed/` (不含 NPZ 样本) |
| XFOIL 工具与构建脚本 | `tools/xfoil/README.md`, 构建命令记录 |
| sdfdata 项目文档 | `sdfdata/SESSION_LOG_*.md`, `UBUNTU_MIGRATION_HANDOFF.md`, `任务.txt` |
| NOTtest 项目文档 | `NOTtest/README.md`, `WORKLOG_*.md`, `docs/` |
| 子午面实验指标 | `Meri-FID-Gate-Airfoil/outputs/timing_summary.*` |

> 注意：模型权重 (`.pt`)、NPZ 样本、FEM 结果等大文件未包含在基础打包中，需单独传输。

---

> 文档生成时间：2026-06-15  
> 基于代码库 commit `01121ba`  
> 撰写人：Claude Code (Anthropic)
