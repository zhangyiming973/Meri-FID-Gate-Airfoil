# UIUC 翼型有限条件扩散生成实验方案

## 1. 实验目标

本实验基于当前 `Meri-FID-Gate` 项目的两阶段生成框架，改造为翼型几何条件扩散生成任务。

现阶段只使用 UIUC Airfoil Coordinates Database 作为训练数据源，条件限定为可从翼型坐标直接计算的几何条件，不强依赖 CFD 数据或高成本气动数据源。

核心目标：

1. 将 UIUC 翼型坐标统一预处理为固定格式。
2. 训练翼型几何自编码器，获得稳定潜空间表示。
3. 在潜空间训练有限几何条件扩散模型。
4. 在给定厚度、弯度等几何条件时生成合法、多样的翼型。
5. 通过几何校核、条件误差和近邻分析评估生成质量。

非目标：

1. 不在第一阶段追求精确 CFD 条件生成。
2. 不强制寻找或构建大规模气动数据库。
3. 不把已有 F404 / single 子午面权重直接迁移为翼型权重。
4. 不在第一版中做完整工业级气动优化闭环。

## 2. 当前项目基础

当前仓库是 `Meridian SDF` 尺寸条件潜空间扩散项目，结构为：

1. 自编码器阶段：`SDF + semantic mask -> latent z_m -> reconstructed SDF`
2. 门控阶段：检查重建质量和潜空间分布。
3. 条件扩散阶段：以设计参数为条件，在潜空间生成 `z_m`，再由自编码器解码成 SDF。

现有统一入口：

```bash
python run.py prepare-splits --dataset <dataset>
python run.py train --scheme <scheme> --dataset <dataset>
python run.py test --scheme <scheme> --run-dir <run_dir>
```

推荐复用的模块：

1. `scripts/prepare_splits.py`
2. `scripts/split_utils.py`
3. `schemes/pca_unet/`
4. `schemes/dim_guided/`
5. `schemes/dim_unet/`
6. `latent_gate`
7. `condition_stats`
8. `cfg_dropout` / `cfg_scale`

第一版推荐优先复用 `pca_unet` 或 `dim_guided` 路线：

1. `pca_unet`：适合将翼型转成二维 SDF 后复用卷积自编码器和 PCA-UNet 扩散。
2. `dim_guided`：适合快速验证低维条件向量是否有效。

## 3. 数据源

### 3.1 主数据源

UIUC Airfoil Coordinates Database。

本地位置：

```text
data/airfoil/raw/uiuc/coord_seligFmt.zip
data/airfoil/raw/uiuc/coord_seligFmt/
```

当前已解压得到约 1665 个翼型坐标文件。

官方来源：

```text
https://m-selig.ae.illinois.edu/ads/coord_database.html
https://m-selig.ae.illinois.edu/ads/archives/coord_seligFmt.zip
```

### 3.2 暂不使用的数据源

G2Aero curated airfoils 体量较大，当前网络下载成本高，本阶段不作为必要依赖。

AirfRANS / CFD 数据源暂不进入主流程。后续如果需要气动条件扩散，可在几何条件扩散跑通后作为扩展阶段接入。

## 4. 数据表示方案

### 4.1 第一版推荐表示：二维 SDF

为最大限度复用当前项目，第一版将翼型轮廓 rasterize 为二维 SDF。

每个翼型样本保存为 `.npz`：

```text
sdf2d_norm: float32, shape = [H, W]
semantic_mask: float32 or int64, shape = [H, W]
coords_raw: float32, shape = [N_raw, 2]
coords_resampled: float32, shape = [2, N]
condition_raw: float32, shape = [K]
```

建议分辨率：

```text
H = 128
W = 256
```

理由：

1. 翼型弦长方向远大于厚度方向，用长宽比网格更合理。
2. `128 x 256` 比 `256 x 256` 更省显存。
3. 第一版先验证生成闭环，不追求最高几何分辨率。

实现约束：

1. 当前 `pca_unet` 自编码器已支持通过 `autoencoder.input_size` 配置输出尺寸。
2. 由于编码器有 4 次 2 倍下采样，`input_size` 必须等于 `latent_spatial` 的 16 倍。
3. 若使用 `128 x 256`，应配置 `latent_spatial = [8, 16]`；若使用兼容旧配置的 `256 x 256`，可配置 `latent_spatial = 16`。

坐标范围建议：

```text
x in [-0.05, 1.05]
y in [-0.25, 0.25]
```

SDF 归一化建议：

```text
sdf_norm = clip(sdf / sdf_scale, -1, 1)
```

其中 `sdf_scale` 可取 `0.05` 到 `0.10` 弦长单位，具体根据重建边界清晰度调参。

### 4.2 备选表示：一维上下表面

后续可改为：

```text
x_grid: cosine spacing, shape = [N]
y_upper: shape = [N]
y_lower: shape = [N]
shape_vector: [2, N]
```

该方案更适合几何精确校核和导出 `.dat`，但会改动现有自编码器和扩散模型输入，不作为第一版主线。

## 5. 预处理流程

### 5.1 解析坐标

对每个 UIUC `.dat` 文件：

1. 读取文件名作为 `sample_id`。
2. 跳过标题行和非数值行。
3. 解析二维坐标点。
4. 删除重复点、NaN、Inf 和明显异常点。
5. 如果有效点数过少，则丢弃。

建议最小有效点数：

```text
N_raw >= 20
```

### 5.2 几何归一化

对每个翼型：

1. 找到前缘点：通常为最小 `x` 点。
2. 找到尾缘点：通常为最大 `x` 点或上下尾缘平均点。
3. 将翼型平移、缩放到弦长为 1。
4. 前缘对齐到 `x=0`，尾缘对齐到 `x=1`。
5. 如有必要，按弦线轻微旋转，使尾缘中心与前缘位于 `y=0` 附近。

归一化后约束：

```text
min(x) ~= 0
max(x) ~= 1
chord ~= 1
```

### 5.3 上下表面拆分

推荐方法：

1. 找到前缘索引。
2. 按坐标序列将轮廓拆成两条曲线。
3. 依据平均 `y` 或中弧线位置判断 upper / lower。
4. 对 upper 和 lower 分别按 `x` 排序。
5. 用统一 `x_grid` 插值。

建议 `x_grid` 使用 cosine spacing：

```text
x_i = 0.5 * (1 - cos(pi * i / (N - 1)))
```

推荐：

```text
N = 257
```

### 5.4 几何条件计算

第一版使用 5 维有限几何条件：

```text
t_max
x_tmax
camber_max
x_camber_max
te_gap
```

定义：

```text
thickness(x) = y_upper(x) - y_lower(x)
camber(x) = 0.5 * (y_upper(x) + y_lower(x))
t_max = max(thickness)
x_tmax = x at max(thickness)
camber_max = max(abs(camber))
x_camber_max = x at max(abs(camber))
te_gap = thickness(x=1)
```

可选补充条件：

```text
area
leading_edge_radius_proxy
trailing_edge_angle_proxy
```

第一版不建议超过 5 到 7 维条件，避免 UIUC 数据规模下条件空间过稀。

### 5.5 样本过滤

过滤规则：

1. `t_max <= 0`
2. `t_max > 0.35`
3. `t_max < 0.02`
4. `te_gap < -1e-4`
5. 上下表面插值失败。
6. `thickness(x)` 大面积为负。
7. 曲线存在明显尖峰或自交。
8. 坐标归一化后超出合理范围过多。

建议记录过滤原因，输出：

```text
data/airfoil/processed/uiuc_sdf/filter_report.json
```

### 5.6 生成二维 SDF

步骤：

1. 用重采样后的 upper/lower 合成闭合多边形。
2. 在固定二维网格上计算每个像素到多边形边界的距离。
3. 多边形内部设为负值，外部设为正值。
4. 裁剪并归一化为 `sdf2d_norm`。
5. `semantic_mask` 第一版可用二值内部区域：

```text
semantic_mask = sdf2d_norm < 0
```

如果不需要语义通道，可将 `use_semantic=false`，只训练单通道 SDF AE。

## 6. 数据目录设计

建议新增数据集名：

```text
airfoil_uiuc_sdf
```

目录结构：

```text
data/airfoil/
  raw/
    uiuc/
      coord_seligFmt.zip
      coord_seligFmt/
  processed/
    uiuc_sdf/
      airfoil_index.csv
      train_split.csv
      test_split.csv
      condition_stats.json
      split_meta.json
      filter_report.json
      airfoil_samples/
        <sample_id>.npz

data/airfoil_uiuc_sdf/
  dataset.json
  processed -> ../airfoil/processed/uiuc_sdf
```

如果现有 `prepare_splits.py` 不支持软链接或跨目录，可直接使用：

```text
data/airfoil_uiuc_sdf/
  dataset.json
  processed/
    airfoil_index.csv
    airfoil_samples/
```

### 6.1 索引 CSV 字段

`airfoil_index.csv` 至少包含：

```text
sample_id
npz_file
source_file
passed_quality
t_max
x_tmax
camber_max
x_camber_max
te_gap
num_raw_points
num_resampled_points
```

### 6.2 dataset.json

示例：

```json
{
  "processed_dir": "processed",
  "index_file": "airfoil_index.csv",
  "npz_dir": "airfoil_samples",
  "condition_columns": [
    "t_max",
    "x_tmax",
    "camber_max",
    "x_camber_max",
    "te_gap"
  ],
  "split": {
    "test_size": 0.15,
    "seed": 42,
    "filter_passed_quality": true
  }
}
```

说明：`test_size` 支持两种语义，`0 < test_size < 1` 表示测试集比例，`test_size >= 1` 表示测试集样本数。

## 7. 训练方案

### 7.1 阶段 A：数据预处理冒烟

目标：

1. 从 UIUC `.dat` 生成至少 100 个有效 `.npz`。
2. 可视化 20 个随机 SDF。
3. 检查条件分布。

输出：

```text
airfoil_index.csv
filter_report.json
condition_histograms.png
sdf_preview_grid.png
```

通过标准：

1. 有效样本数大于 1000。
2. 大多数翼型轮廓方向正确。
3. SDF 内外符号正确。
4. 条件分布没有明显异常长尾未处理。

### 7.2 阶段 B：自编码器训练

目标：

训练翼型 SDF 自编码器，使其能稳定重建翼型边界。

推荐配置：

```json
{
  "autoencoder": {
    "in_channels": 1,
    "latent_channels": 64,
    "latent_spatial": [8, 16],
    "input_size": [128, 256],
    "epochs": 100,
    "batch_size": 16,
    "lr": 0.0003,
    "use_semantic": false,
    "loss_weights": {
      "l1": 1.0,
      "l2": 0.5,
      "zero_level": 3.0,
      "zero_sigma_mm": 1.0,
      "dimension": 0.0,
      "semantic": 0.0
    }
  }
}
```

说明：

1. 当前项目中的 `dimension` loss 是面向子午面厚度/面积的代理项，第一版翼型可以先关掉。
2. `zero_level` 对 SDF 边界重建重要，建议保留并提高权重。
3. 第一版先用单通道 SDF，减少语义 mask 适配成本。
4. 如果临时使用 `256 x 256` 数据，需把 `input_size` 改为 `[256, 256]`，并把 `latent_spatial` 改为 `16` 或 `[16, 16]`。

通过标准：

1. 测试集 SDF L1 明显收敛。
2. 重建图中前缘和尾缘没有大面积破损。
3. 从重建 SDF 提取轮廓后，`t_max` 和 `camber_max` 误差可接受。
4. `latent_gate` 通过或报告中没有明显潜空间塌缩。

### 7.3 阶段 C：有限条件扩散训练

目标：

在 AE 潜空间中训练条件扩散模型，使模型能根据 5 维几何条件生成翼型。

推荐优先方案：

```text
scheme = pca_unet
stage = unet
```

推荐配置：

```json
{
  "unet": {
    "pca_dim": 128,
    "unet_base_ch": 64,
    "timesteps": 200,
    "epochs": 300,
    "batch_size": 32,
    "lr": 0.0003,
    "cfg_dropout": 0.2,
    "cfg_scale": 1.5,
    "sample_steps": 50,
    "use_ddim": true
  }
}
```

训练命令形态：

```bash
python run.py train --scheme pca_unet --dataset airfoil_uiuc_sdf --stage ae
python run.py train --scheme pca_unet --dataset airfoil_uiuc_sdf --stage unet --ae-run-dir outputs/pca_unet/airfoil_uiuc_sdf/<timestamp>/autoencoder
```

第一版也可以直接：

```bash
python run.py train --scheme pca_unet --dataset airfoil_uiuc_sdf
```

通过标准：

1. 训练损失稳定下降。
2. 测试条件下生成 SDF 不为空、不塌缩。
3. 生成样本通过几何合法性过滤的比例大于 60%。
4. 条件误差优于随机采样基线。

## 8. 采样与生成

### 8.1 条件采样策略

从训练集条件分布中构建有限目标集合：

1. 训练集真实条件重放：使用测试集条件生成。
2. 网格条件生成：厚度和弯度做低维网格。
3. 插值条件生成：在两个真实翼型条件之间线性插值。

推荐网格：

```text
t_max: [0.08, 0.10, 0.12, 0.15, 0.18]
camber_max: [0.00, 0.02, 0.04, 0.06]
x_tmax: [0.25, 0.30, 0.35, 0.40]
x_camber_max: [0.30, 0.40, 0.50]
te_gap: [0.0]
```

注意：

1. 不建议第一版生成训练集分布外太远的条件。
2. 每个条件生成多个候选，例如 32 或 64 个。
3. 使用不同 `cfg_scale` 比较条件控制性和多样性。

### 8.2 CFG scale 对比

建议测试：

```text
cfg_scale = 0.0, 1.0, 1.5, 2.0, 3.0
```

评估：

1. `cfg_scale` 过低时条件误差可能大。
2. `cfg_scale` 过高时多样性可能下降，非法样本可能增加。
3. 最优点应通过合法率、条件误差和多样性共同选择。

### 8.3 输出格式

每次生成保存：

```text
outputs/pca_unet/airfoil_uiuc_sdf/<timestamp>/airfoil_generations/
  samples.npz
  samples.csv
  previews/
  dat/
  metrics.json
```

其中：

```text
samples.npz:
  sdf
  coords
  condition_target
  condition_measured

samples.csv:
  sample_id
  condition_target_*
  condition_measured_*
  validity
  nearest_neighbor_id
  nearest_neighbor_distance
```

## 9. 几何校核

### 9.1 从 SDF 还原轮廓

对生成 SDF：

1. 提取零水平集轮廓。
2. 选取最大闭合轮廓。
3. 归一化到弦长坐标。
4. 转换为 upper/lower 表面。
5. 重采样到统一 `x_grid`。

如果提取不到闭合轮廓，则判为无效。

### 9.2 合法性检查

生成样本必须检查：

1. 是否存在有效闭合轮廓。
2. 是否能拆分 upper/lower。
3. `thickness(x) >= -epsilon` 的比例是否足够高。
4. `t_max` 是否在合理范围内。
5. `te_gap` 是否非负且不过大。
6. 是否存在自交。
7. 前缘是否断裂。
8. 尾缘是否异常开口。
9. 曲率是否存在尖峰。
10. 面积是否大于最小阈值。

建议阈值：

```text
0.02 <= t_max <= 0.35
abs(camber_max) <= 0.20
te_gap <= 0.05
negative_thickness_fraction <= 0.01
```

### 9.3 条件一致性指标

对每个有效生成样本，重新计算几何条件：

```text
t_max_gen
x_tmax_gen
camber_max_gen
x_camber_max_gen
te_gap_gen
```

计算：

```text
MAE(t_max)
MAE(x_tmax)
MAE(camber_max)
MAE(x_camber_max)
MAE(te_gap)
condition_success_rate
```

其中 `condition_success_rate` 可定义为：

```text
abs(t_max_error) <= 0.01
abs(camber_error) <= 0.01
abs(x_tmax_error) <= 0.08
abs(x_camber_error) <= 0.10
```

### 9.4 多样性与新颖性指标

将生成翼型和训练翼型都转换为统一一维向量：

```text
shape_vector = concat(y_upper, y_lower)
```

计算：

1. 最近邻距离。
2. 重复率。
3. 与训练集最近邻可视化。
4. 条件桶内多样性。
5. PCA / t-SNE 分布图。

建议报告：

```text
validity_rate
unique_rate
novelty_rate
mean_nearest_neighbor_distance
condition_mae
condition_success_rate
```

## 10. 可选轻量气动检查

第一版不依赖 CFD，但可以保留一个可选 sanity check。

如果本地环境容易安装并运行 XFoil，可对少量 top-K 样本做快速评估：

```text
AoA = 0, 2, 4, 6 deg
Re = 1e6
Mach = 0.0
```

输出：

```text
xfoil_converged
Cl
Cd
Cm
Cl/Cd
```

注意：

1. XFoil 结果只作为粗筛，不作为训练监督。
2. 对失速、分离和复杂流动不作强结论。
3. 如果安装或收敛成本高，直接跳过，不影响主实验。

## 11. 实验矩阵

### 11.1 基线实验

| 实验名 | 数据 | 条件 | 模型 | 目的 |
| --- | --- | --- | --- | --- |
| `ae_sdf_baseline` | UIUC SDF | 无 | AE | 验证 SDF 重建 |
| `diff_uncond` | UIUC SDF latent | 无 | latent diffusion | 验证无条件生成 |
| `diff_cond_5d` | UIUC SDF latent | 5D 几何 | conditional diffusion | 验证条件生成 |
| `diff_cond_5d_cfg` | UIUC SDF latent | 5D 几何 | CFG diffusion | 验证条件引导强度 |

### 11.2 消融实验

| 实验名 | 变量 | 评价 |
| --- | --- | --- |
| `sdf_128x256_vs_256x256` | SDF 分辨率 | 重建误差、生成合法率 |
| `pca_dim_64_128_192` | PCA 维度 | 生成质量、多样性 |
| `cfg_scale_sweep` | CFG scale | 条件误差、多样性 |
| `condition_3d_vs_5d` | 条件维度 | 控制性和稳定性 |

### 11.3 推荐第一轮最小实验

只跑：

1. `ae_sdf_baseline`
2. `diff_cond_5d`
3. `cfg_scale_sweep`

这样可以最快判断这条路线是否成立。

## 12. 评估报告结构

每轮实验输出一份报告：

```text
reports/airfoil_uiuc_sdf/<run_id>/
  summary.md
  metrics.json
  condition_error.csv
  generated_samples_grid.png
  nearest_neighbors.png
  condition_scatter.png
  validity_breakdown.png
```

`summary.md` 建议包含：

1. 数据样本数量。
2. 过滤数量和原因。
3. AE 重建指标。
4. 扩散训练指标。
5. 生成合法率。
6. 条件误差。
7. 多样性和新颖性。
8. 代表性成功样本。
9. 代表性失败样本。
10. 下一轮修改建议。

## 13. 风险与应对

### 13.1 UIUC 数据规模偏小

风险：

UIUC 只有约 1665 个翼型，条件扩散可能过拟合或生成多样性有限。

应对：

1. 控制条件维度。
2. 使用数据增强，例如轻微厚度缩放、弯度缩放。
3. 使用 nearest-neighbor 检查避免复刻。
4. 第一版只要求有限条件生成，不要求复杂外推。

### 13.2 SDF 转回坐标有误差

风险：

二维 SDF 的零水平集提取可能导致前缘/尾缘细节损失。

应对：

1. 使用 `128x256` 或 `256x512` 非方形高分辨率。
2. 提高 zero-level loss 权重。
3. 对导出的轮廓做轻量平滑。
4. 后续改为 1D 坐标扩散。

### 13.3 条件控制不稳定

风险：

生成样本满足整体翼型分布，但条件误差较大。

应对：

1. 增加 CFG scale。
2. 加入条件回归辅助损失。
3. 引入 rejection sampling。
4. 使用生成后几何 gate 排序。

### 13.4 旧项目损失函数不完全适配翼型

风险：

当前 `dimension` / `physics_guidance` 面向原子午面任务，直接使用可能误导翼型训练。

应对：

1. 第一版关闭 `dimension` loss 和 `physics_guidance`。
2. 使用翼型专用几何校核作为后处理 gate。
3. 第二版再将厚度、弯度误差接入训练损失。

## 14. 建议实施里程碑

### M1：数据预处理完成

交付：

1. `airfoil_index.csv`
2. `.npz` SDF 样本
3. 条件统计文件
4. 预处理报告图

验收：

1. 有效样本数大于 1000。
2. 随机可视化样本轮廓正确。

### M2：AE 重建跑通

交付：

1. `best_autoencoder.pt`
2. 重建可视化
3. 重建误差报告

验收：

1. 测试集重建边界清晰。
2. 前缘和尾缘多数样本可接受。

### M3：条件扩散跑通

交付：

1. `best_unet_diffusion.pt`
2. 条件生成样本
3. CFG 对比报告

验收：

1. 生成合法率大于 60%。
2. 条件误差优于随机训练集近邻基线。

### M4：完整实验报告

交付：

1. 生成样本网格图。
2. 条件误差表。
3. 最近邻分析图。
4. 失败案例分析。
5. 下一阶段建议。

验收：

1. 能给定有限几何条件生成可用翼型。
2. 能解释模型在哪些条件区域表现好或差。

## 15. 第一版推荐命令流程

以下为预期流程，具体脚本名称以后续实现为准。

```bash
# 1. 预处理 UIUC 翼型为 SDF 数据集
python scripts/airfoil/preprocess_uiuc_sdf.py \
  --raw-dir data/airfoil/raw/uiuc/coord_seligFmt \
  --out-dir data/airfoil_uiuc_sdf/processed \
  --height 128 \
  --width 256 \
  --points 257

# 2. 生成固定训练/测试划分
python run.py prepare-splits --dataset airfoil_uiuc_sdf --force

# 3. 训练 AE
python run.py train \
  --scheme pca_unet \
  --dataset airfoil_uiuc_sdf \
  --stage ae

# 4. 训练条件扩散
python run.py train \
  --scheme pca_unet \
  --dataset airfoil_uiuc_sdf \
  --stage unet \
  --ae-run-dir outputs/pca_unet/airfoil_uiuc_sdf/<timestamp>/autoencoder

# 5. 测试与可视化
python run.py test \
  --scheme pca_unet \
  --run-dir outputs/pca_unet/airfoil_uiuc_sdf/<timestamp>

# 6. 条件采样与几何校核
python scripts/airfoil/sample_airfoils.py \
  --run-dir outputs/pca_unet/airfoil_uiuc_sdf/<timestamp> \
  --condition-grid configs/airfoil/condition_grid_5d.json \
  --num-per-condition 64 \
  --cfg-scale 1.5

python scripts/airfoil/evaluate_generated_airfoils.py \
  --samples outputs/pca_unet/airfoil_uiuc_sdf/<timestamp>/airfoil_generations/samples.npz \
  --train-index data/airfoil_uiuc_sdf/processed/train_split.csv
```

## 16. 成功判据

第一版实验达到以下结果即可认为成立：

1. UIUC 数据预处理有效样本数大于 1000。
2. AE 能稳定重建大部分翼型。
3. 条件扩散能生成非空、非塌缩翼型。
4. 有效生成样本合法率大于 60%。
5. `t_max` 和 `camber_max` 平均误差小于 0.015 到 0.02。
6. 生成样本不是简单复刻训练集，最近邻距离分布有合理间隔。
7. 可以导出 `.dat` 坐标文件供后续分析使用。

如果达不到上述标准，优先排查顺序：

1. SDF 预处理是否正确。
2. AE 重建是否足够好。
3. 条件标准化是否正确。
4. CFG scale 是否过高或过低。
5. PCA 维度是否过小。
6. UIUC 中异常样本是否过滤不足。
