# UIUC 翼型条件扩散编码计划

## 目标

按测试优先原则，把 UIUC 翼型数据预处理、SDF 数据集接入、`pca_unet` 自编码器训练和后续条件扩散训练准备成结构清晰的五个提交。

当前机器没有 GPU，因此本阶段只要求 CPU 上的小样本单测、编译和 smoke test 通过。后续实验机器有 GPU/CUDA 时，再执行完整 AE 与 diffusion 训练。

## 设计原则

1. 几何算法、SDF 栅格化、文件批处理、训练入口分层实现。
2. 不把 UIUC 专属逻辑写进 `schemes/pca_unet` 的模型和训练循环。
3. 所有条件列从 `dataset.json`、`condition_stats.json` 或显式参数传入，不新增全局硬编码。
4. 测试先行：每个提交先加或更新测试，再实现最小代码。
5. CPU 环境只做快速验证；GPU 相关配置只预留，不在 CPU 机器上强制运行。

## 提交 1：翼型几何核心与测试

### 新增

1. `tests/fixtures/airfoil/simple_symmetric.dat`
2. `tests/fixtures/airfoil/simple_cambered.dat`
3. `tests/test_airfoil_geometry.py`
4. `scripts/airfoil_geometry.py`

### 测试先写

覆盖以下行为：

1. UIUC/Selig `.dat` 解析能跳过标题行、空行和非数值行。
2. 坐标归一化后前缘接近 `x=0`，尾缘接近 `x=1`。
3. 上下表面拆分后输出稳定的 `upper/lower`。
4. cosine `x_grid` 重采样后厚度大部分非负。
5. 五维条件可计算：
   - `t_max`
   - `x_tmax`
   - `camber_max`
   - `x_camber_max`
   - `te_gap`

### 实现范围

`scripts/airfoil_geometry.py` 只做纯数组逻辑，不读写批量数据：

1. `parse_selig_dat(path: Path) -> np.ndarray`
2. `normalize_airfoil(coords: np.ndarray) -> np.ndarray`
3. `split_surfaces(coords: np.ndarray) -> AirfoilSurfaces`
4. `cosine_x_grid(n: int = 257) -> np.ndarray`
5. `resample_surfaces(surfaces, x_grid) -> ResampledAirfoil`
6. `compute_airfoil_conditions(resampled) -> dict[str, float]`
7. `validate_airfoil(resampled, conditions) -> list[str]`

### 验证命令

```bash
/home/vipuser/miniconda3/bin/python -m pytest tests/test_airfoil_geometry.py
/home/vipuser/miniconda3/bin/python -m compileall scripts/airfoil_geometry.py
```

## 提交 2：翼型 SDF 栅格化与测试

### 新增

1. `tests/test_airfoil_sdf.py`
2. `scripts/airfoil_sdf.py`

### 测试先写

覆盖以下行为：

1. 简单翼型轮廓可生成闭合多边形。
2. 输出 `sdf2d_norm.shape == (128, 256)`。
3. `semantic_mask.shape == (128, 256)`。
4. `semantic_mask` 是二值 mask。
5. 内部 SDF 为负，外部 SDF 为正。
6. SDF 值被裁剪到 `[-1, 1]`。

### 实现范围

`scripts/airfoil_sdf.py` 不关心 UIUC 文件系统布局，只处理重采样后的翼型：

1. `build_airfoil_polygon(resampled) -> np.ndarray`
2. `make_sdf_grid(height, width, bounds) -> tuple[np.ndarray, np.ndarray]`
3. `signed_distance_to_polygon(points, polygon) -> np.ndarray`
4. `rasterize_airfoil_sdf(resampled, height=128, width=256, bounds=(-0.05, 1.05, -0.25, 0.25), sdf_scale=0.08) -> dict`

### 验证命令

```bash
/home/vipuser/miniconda3/bin/python -m pytest tests/test_airfoil_sdf.py
/home/vipuser/miniconda3/bin/python -m compileall scripts/airfoil_sdf.py
```

## 提交 3：UIUC 预处理流水线与数据集元信息

### 新增

1. `tests/test_prepare_airfoil_uiuc.py`
2. `scripts/prepare_airfoil_uiuc.py`
3. `data/airfoil_uiuc_sdf/dataset.json`
4. `schemes/pca_unet/config/airfoil_uiuc_sdf.json`

### 修改

1. `run.py`

### 测试先写

用临时目录和 fixture `.dat` 验证：

1. 预处理能生成 `airfoil_index.csv`。
2. 预处理能生成 `airfoil_samples/<sample_id>.npz`。
3. `.npz` 包含：
   - `sdf2d_norm`
   - `semantic_mask`
   - `coords_raw`
   - `coords_resampled`
   - `condition_raw`
   - `condition_json`
4. index 包含：
   - `sample_id`
   - `npz_file`
   - `source_file`
   - `passed_quality`
   - 五维条件列
5. `filter_report.json` 记录通过和失败样本数。

### 实现范围

`scripts/prepare_airfoil_uiuc.py` 只做批处理编排：

1. 发现 UIUC `.dat` 文件。
2. 调用 `airfoil_geometry.py` 和 `airfoil_sdf.py`。
3. 写 `.npz`。
4. 写 index CSV。
5. 写 filter report。

`run.py` 新增子命令：

```bash
python run.py prepare-airfoil-uiuc
```

### GPU/CUDA 预留

`schemes/pca_unet/config/airfoil_uiuc_sdf.json` 中保留：

```json
{
  "device": "cuda",
  "autoencoder": {
    "input_size": [128, 256],
    "latent_spatial": [8, 16],
    "use_semantic": false
  },
  "num_workers": 0
}
```

CPU smoke test 可通过 `--fast` 和小样本数据运行；完整训练后续在 CUDA 机器上执行。

### 验证命令

```bash
/home/vipuser/miniconda3/bin/python -m pytest tests/test_prepare_airfoil_uiuc.py
/home/vipuser/miniconda3/bin/python run.py prepare-airfoil-uiuc --help
```

## 提交 4：固定划分、数据集读取与 AE CPU smoke test

### 新增

1. `tests/test_pca_unet_airfoil_dataset.py`
2. `tests/test_pca_unet_airfoil_smoke.py`

### 修改

1. `schemes/pca_unet/train/train_ae.py`
2. `schemes/pca_unet/pipeline.py`
3. 必要时修改 `schemes/pca_unet/data/dataset.py`

### 测试先写

覆盖以下行为：

1. `prepare-splits` 对 `test_size: 0.15` 生成正确比例。
2. `MeridianSDFDataset` 读取翼型 `.npz` 时：
   - 条件列来自 `condition_stats.columns`
   - `use_semantic=false` 时 `x` 是单通道
   - 缺失 `physics_summary_json` 不失败
3. AE 对 `[128, 256]` 输入输出尺寸一致。
4. CPU 上可以跑 1 个 epoch 的极小训练 batch，不要求收敛。

### 实现范围

1. 将 AE 尺寸配置校验前置，错误信息明确。
2. 如果 `loss_weights.dimension == 0`，避免读取或要求 physics 字段。
3. `pipeline.py` 增加配置校验：
   - `input_size / latent_spatial == 16`
   - `condition_columns` 非空

### GPU/CUDA 预留

1. 训练代码保留当前逻辑：`cfg.device == "cuda"` 且 `torch.cuda.is_available()` 时使用 CUDA。
2. 不在 CPU 测试中强制 mock CUDA。
3. 在配置中保留 CUDA 默认值，CPU 机器自动回退到 CPU。

### 验证命令

```bash
/home/vipuser/miniconda3/bin/python -m pytest tests/test_pca_unet_airfoil_dataset.py tests/test_pca_unet_airfoil_smoke.py
/home/vipuser/miniconda3/bin/python run.py prepare-splits --dataset airfoil_uiuc_sdf --force
/home/vipuser/miniconda3/bin/python run.py train --scheme pca_unet --dataset airfoil_uiuc_sdf --stage ae --fast
```

最后一条只在小样本 fixture 或临时数据集上跑，不在全量 UIUC 上跑。

## 提交 5：扩散阶段接入与 GPU 实验准备

### 新增

1. `tests/test_pca_unet_airfoil_diffusion_config.py`
2. `docs/airfoil_gpu_runbook.md` 或在 `编码计划.md` 后续补充 GPU 运行记录段。

### 修改

1. `schemes/pca_unet/train/train_diffusion.py`
2. `schemes/pca_unet/test.py`
3. `schemes/pca_unet/config/airfoil_uiuc_sdf.json`

### 测试先写

覆盖以下行为：

1. diffusion 加载 AE checkpoint 时使用 checkpoint 内的 `input_size` 和 `latent_spatial`。
2. `build_pca_unet` 的条件维度等于 `condition_stats.columns` 长度。
3. 小型 fake latent records 可以构造 diffusion dataloader。
4. CPU 上只跑一两个 batch 的 loss 前向，不做完整采样训练。

### 实现范围

1. 扩散阶段对翼型数据不引入新硬编码。
2. 确认 `cfg_dropout`、`cfg_scale`、`sample_steps` 对 airfoil 配置生效。
3. 评估阶段先输出 SDF 图和基础 L1，不急于加入复杂几何反提取指标。

### GPU/CUDA 预留

准备 GPU 实验命令：

```bash
python run.py prepare-airfoil-uiuc --force
python run.py prepare-splits --dataset airfoil_uiuc_sdf --force
python run.py train --scheme pca_unet --dataset airfoil_uiuc_sdf --stage ae
python run.py train --scheme pca_unet --dataset airfoil_uiuc_sdf --stage unet --ae-run-dir outputs/pca_unet/airfoil_uiuc_sdf/<timestamp>/autoencoder
python run.py test --scheme pca_unet --run-dir outputs/pca_unet/airfoil_uiuc_sdf/<timestamp>
```

GPU 机器上需要记录：

1. CUDA 版本。
2. `torch.cuda.is_available()`。
3. GPU 型号与显存。
4. batch size。
5. AE 峰值显存。
6. diffusion 峰值显存。
7. 每阶段训练时间。

### 验证命令

```bash
/home/vipuser/miniconda3/bin/python -m pytest tests/test_pca_unet_airfoil_diffusion_config.py
/home/vipuser/miniconda3/bin/python -m compileall schemes/pca_unet/train/train_diffusion.py schemes/pca_unet/test.py
```

## 执行顺序

1. 提交 1：几何核心。
2. 提交 2：SDF 栅格化。
3. 提交 3：UIUC 预处理入口与数据集配置。
4. 提交 4：数据集读取与 AE CPU smoke test。
5. 提交 5：扩散阶段接入与 GPU runbook。

每个提交都必须包含：

1. 新增或更新测试。
2. 最小实现。
3. 本地验证命令输出记录。
4. 清晰提交信息。

## 当前环境备注

当前机器：

1. 无 GPU，不执行全量训练。
2. 已安装 `pandas` 到当前用户 Python 环境。
3. 可用 Python 路径优先使用：

```bash
/home/vipuser/miniconda3/bin/python
```

后续 GPU 机器：

1. 保持 `device: "cuda"` 配置。
2. 训练代码应自动检测 CUDA；无 CUDA 时回退 CPU。
3. 不把 CUDA-only 假设写入测试。
